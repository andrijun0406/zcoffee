# G*lden Image ISOs (never commit)
iso*/
*.iso

# Installers
*.exe
*.msi
*# Run logs
logs/
scripts/powershel*/logs/
*.log

# Real deployment pa*ameter files containing tenant/sub*cription/secrets
scripts/arm-templ*tes/odin-parameters.json
scripts/a*m-templates/*.local.json
*.local.j*on

# Local admin / secret materia*
*.secret
*.secret.local
*.pfx
*.k*y

# Unattended install answer-fil* artifacts
autounattend.iso
Autoun*ttend.xml
autounattend.xml
*autoun*ttend*.iso
*unattend*.iso
AzureLoc*l-unattend.iso

# DPAPI node crede*tial store
config/*.secret.local
`*